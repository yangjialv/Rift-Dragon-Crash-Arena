# Rift Dragon Crash Arena 开发协作计划

> 版本：v1.0  
> 制定日期：2026-07-25  
> 当前引擎：Unreal Engine 5.3  
> 项目目录：`rdca/`  
> 玩法基准：`核心玩法.md v0.2`

---

## 1. 计划目标

本计划用于把当前可移动、可平面冲撞的 UE5 原型，逐步推进为一个可运行、可演示、可测试的 Boss 战 Demo。

最终最小闭环为：

```text
地面走位与低弧冲刺
→ Boss 地面冲击波
→ 玩家高弧跳跃躲避
→ 玩家附着空间锚点
→ Boss 弱点暴露
→ 玩家从锚点重撞弱点
→ 按接触点与法线反弹落地
→ 重复三轮击败 Boss
```

开发过程中始终遵循：

1. 每次只增加一个可验证的玩法能力；
2. C++ 负责核心规则、状态、碰撞、日志和测试接口；
3. Blueprint 负责资产装配、表现、参数配置和关卡事件；
4. 每个阶段都必须在编辑器中实际 Play 验收；
5. 当前阶段通过后再提交 Git，并进入下一阶段；
6. P0 闭环完成前不投入复杂模型、Niagara 和场景润色。

---

## 2. 当前设计裁定

### 2.1 当前权威版本

当旧企划书与 `核心玩法.md v0.2` 发生冲突时，P0 开发以 `核心玩法.md v0.2` 为准。

### 2.2 护盾与锚点规则

旧企划中“撞击节点削减 Boss 护盾”的规则暂不进入 P0。

P0 采用：

```text
Boss 攻击结束
→ 弱点短暂暴露
→ 玩家必须借助锚点完成重撞
→ 有效命中扣除 Boss 1 点 HP
```

空间锚点在 P0 中的主要职责是：

1. 提供附着位置；
2. 提供 Z 轴高度调整；
3. 提供重撞蓄力起点；
4. 让玩家能够命中高处弱点。

护盾削减可以在 P0 稳定后作为扩展重新加入，不能阻塞最小战斗闭环。

### 2.3 三种移动状态

玩家采用三种空间移动模式：

| 模式 | 运动规则 | 主要用途 |
|---|---|---|
| ArenaPlane | 在竞技场 X-Y 平面移动 | 普通走位 |
| CrashFlight | 沿真实三维抛物线运动 | 低弧冲刺、高弧跳跃、重撞 |
| AttachedSurface | 沿指定锚点表面移动，可改变 Z | 调整攻击高度、等待反击窗口 |

### 2.4 贴地冲刺与抛物线跳跃的控制

第一版采用以下控制方式：

1. 鼠标右键立即执行无起跳的贴地冲刺；
2. 鼠标左键按住/松开执行蓄力抛物线跳跃；
3. 鼠标地面目标决定移动方向和水平距离；
4. 左键蓄力时间决定弹射强度与弧线高度；
5. 从锚点弹出时使用更高的重撞倍率；
6. 贴地冲刺与抛物线跳跃共享状态互斥，但使用各自的速度和冷却参数。

### 2.5 冲撞响应分类

所有可碰撞目标明确返回一种响应，不通过类名或标签散乱判断：

| 响应 | 对象 | 结果 |
|---|---|---|
| Block | 场地边界、普通障碍 | 停止冲撞 |
| Attach | 空间锚点、指定附着面 | 进入 Attached 状态 |
| Rebound | Boss 身体、弱点、攻击目标 | 根据接触法线反弹 |
| Ignore | 纯表现对象 | 不影响冲撞 |

---

## 3. 固定协作流程

每个阶段按照以下顺序执行：

1. **共同确认目标**：只确认本阶段规则和验收标准。
2. **玩家侧准备**：你在 UE 编辑器中创建本阶段需要的 Input、Blueprint、材质或关卡占位资产。
3. **保存并关闭编辑器**：避免 C++ DLL 被占用。
4. **代码实现**：我实现或调整 C++，检查 Git 差异并运行完整编译。
5. **蓝图装配**：你重新打开编辑器，以新的 C++ 类为父类创建或更新蓝图，并配置资产引用。
6. **Play 验收**：你按本计划给出的用例实际操作并反馈现象。
7. **问题修复**：我根据日志、截图或现象定位；必要时你调整资源与参数。
8. **Git 检查点**：通过验收后，你提交并推送当前阶段。

建议每次提交前执行：

```powershell
git status
git diff --check
git add .
git commit -m "<本阶段提交信息>"
git push
```

---

## 4. 阶段 0：保存当前冲撞原型

### 状态

进行中。

当前已经具备：

- C++ 玩家 Pawn、PlayerController 和 GameMode；
- Enhanced Input 与 WASD 移动；
- `UPhaseCrashComponent`；
- 平面蓄力冲撞；
- Ready、Charging、Crashing、Recovery、Cooldown 状态；
- 青色调试瞄准线；
- `IA_Move`、`IA_Crash` 和 `IMC_Player`；
- `Arena_Level` 与玩家蓝图。

### 你负责

1. 最后确认 WASD 和当前平面冲撞仍能运行；
2. 保存 `BP_PlayerCore`、`IMC_Player` 和 `Arena_Level`；
3. 提交并推送当前版本。

建议提交信息：

```text
feat: add phase crash prototype
```

### 我负责

1. 当前 C++ 已完成编译验证；
2. 在进入阶段 1 前检查工作区是否只包含预期改动；
3. 以当前实现作为抛物线改造前的可回退检查点。

### 验收

- WASD 正常；
- 左键蓄力和释放正常；
- 冷却正常；
- 当前版本已推送 Git。

---

## 5. 阶段 1：贴地冲刺与真实抛物线跳跃

### 目标

把当前单一冲撞拆分为右键贴地冲刺与左键真实抛物线跳跃，并加入离开高处后的重力下落。

### 你负责

1. 暂不制作正式特效；
2. 在 `Arena_Level` 放置数个高度容易比较的 Cube；
3. 创建两种测试障碍：
   - 低矮障碍：高弧能够越过；
   - 较宽障碍：低弧需要横向绕开；
4. 保证地面和障碍物使用简单碰撞；
5. 测试时记录短按与长按的大致落点和最高高度。

### 我负责

1. 重构 `UPhaseCrashComponent` 的轨迹计算；
2. 实现右键零弧度贴地冲刺；
3. 用归一化飞行时间计算左键跳跃的 X-Y 位移和 Z 轴抛物线；
4. 全程使用 Sweep，避免穿过障碍或目标；
5. 非飞行状态下应用重力，离开高处后自然落地；
6. 将冲刺距离、速度、最大弧高和飞行时间暴露给蓝图调参；
7. 保持 Recovery、Cooldown 和日志；
8. 增加轨迹调试线和预测落点；
9. 编译验证。

### 验收

- 右键明显表现为快速、无起跳的贴地冲刺；
- 左键蓄力产生真实 Z 位移；
- 玩家能越过低矮障碍；
- 玩家从障碍顶部走出后会自然下落；
- 飞行结束后稳定回到预期高度；
- 轨迹过程中不会穿过阻挡碰撞；
- WASD、恢复和冷却没有回归问题。

### Git 检查点

```text
feat: add low and high arc crash trajectories
```

---

## 6. 阶段 2：冲撞响应接口与测试目标

### 目标

建立 Block、Attach、Rebound、Ignore 四种统一冲撞响应，并先用测试物体验证。

### 你负责

1. 创建 `Blueprints/Test/BP_CrashBlockTarget`；
2. 创建 `Blueprints/Test/BP_CrashAttachTarget`；
3. 创建 `Blueprints/Test/BP_CrashReboundTarget`；
4. 使用灰、蓝、红三种简单材质区分；
5. 将三个目标放入 `Arena_Level`；
6. 调整碰撞体，使目标易于从多个角度撞击。

### 我负责

1. 创建统一的冲撞响应枚举和接口/组件；
2. 在 `UPhaseCrashComponent` 中集中处理响应；
3. Block：结束冲撞并进入恢复；
4. Attach：保存目标、碰撞点和表面法线；
5. Rebound：根据入射方向和表面法线计算反射方向；
6. 反弹方向压制不必要的异常 Z 分量；
7. 输出入射方向、接触点、法线、响应类型和反弹方向日志；
8. 提供蓝图事件用于播放命中反馈；
9. 编译验证。

### 验收

- 撞灰色目标会停止；
- 撞蓝色目标会进入 Attached 状态；
- 从不同角度撞红色目标会产生不同且合理的反弹方向；
- 同一次碰撞只结算一次；
- 未配置响应的对象不会导致崩溃。

### Git 检查点

```text
feat: add crash collision responses
```

---

## 7. 阶段 3：空间锚点与附着移动

### 目标

让玩家能够附着指定锚点，并沿锚点表面改变高度，为攻击 Boss 高处弱点做准备。

### 你负责

1. 创建 `Blueprints/Arena/BP_SpaceAnchor`；
2. 使用长方体作为第一版锚点；
3. 配置蓝色发光占位材质；
4. 在竞技场左右两侧各放置一个锚点；
5. 添加一个 `Attachable Box`，令它完整包裹锚点 Static Mesh，并将 Static Mesh 自身碰撞设为 `NoCollision`；
6. 调整相机和摆放位置，保证高处玩家仍然可见。

### 我负责

1. 提供一个完整的 `Attachable Box` 碰撞组件；
2. 将 Attached 纳入玩家正式状态；
3. 根据命中法线自动识别 Box 六个面中的当前面，并建立该面的二维参考系；
4. Attached 状态下：
   - W/S 控制沿表面上升和下降；
   - A/D 控制允许范围内的横向移动；
   - 玩家保持与表面固定距离；
5. 输入修改 Box 本地接触点，再通过组件 Transform 转换为世界位置；
6. 不使用 `AttachToActor`，由代码维护玩家与表面的相对关系；
7. 地面或其他阻挡物仍通过玩家 Sweep 阻止移动；
8. 使用 `BoxExtent` 作为真实表面边界；接触点越过共享边时自动切换到相邻面，不需要手工配置六个薄框或邻接引用；
9. 支持主动脱离和从附着状态再次蓄力弹出；
10. 附着时暂停普通 `UFloatingPawnMovement`；
11. 提供附着、脱离和表面移动日志；
12. 编译验证。

### 验收

- 只有标记为 Attach 的锚点可以附着；
- 玩家附着后不会抖动或陷入表面；
- W/S 能稳定改变世界 Z；
- 玩家能在当前 Box 面内连续稳定移动；
- 到达共享边后能自动切换到相邻面，输入方向连续且不发生闪现；
- 地面和其他阻挡物可以阻止继续移动；
- 不会越出 BoxExtent 后继续在无限延伸的平面上移动；
- 从不同高度弹出时起点正确；
- 脱离后能恢复地面或飞行控制。

### Git 检查点

```text
feat: add anchor attachment movement
```

---

## 8. 阶段 4：Boss 占位、身体响应与弱点

### 目标

建立可被重撞的 Boss 占位体，并验证锚点高度确实影响 Boss 交互。

### 你负责

1. 创建 `Blueprints/Boss/BP_RiftDragonBoss`；
2. 使用基础几何体搭建巨大龙首占位；
3. 创建：
   - Boss 身体碰撞体；
   - 额头弱点碰撞体；
   - 嘴部攻击起点；
4. Boss 身体与弱点使用明显不同的材质；
5. 将额头弱点放在地面低弧无法直接命中的高度；
6. 从关卡中的锚点确认能够看清并瞄准弱点。

### 我负责

1. 创建 `ARiftDragonBoss`；
2. 创建 Boss HP 与 WeakPoint 状态；
3. Boss 身体返回 Rebound，但不造成伤害；
4. WeakPoint 仅在 Exposed 时接受有效重撞；
5. 有效伤害条件至少包括：
   - 玩家处于 Crashing；
   - 命中 WeakPoint；
   - WeakPoint 为 Exposed；
   - 冲撞来自锚点重撞或达到规定强度；
   - Boss 未死亡；
6. 根据接触点与表面法线进行反弹；
7. Boss HP 默认采用 3 次命中制；
8. 提供弱点状态和 HP 变化蓝图事件；
9. 编译验证。

### 验收

- 地面低弧不能轻易命中额头弱点；
- 玩家可从锚点合适高度重撞弱点；
- 撞 Boss 身体不扣血并反弹；
- 弱点未暴露时不扣血并反弹；
- 弱点暴露且条件满足时 HP 减 1；
- 一次冲撞不能重复扣血。

### Git 检查点

```text
feat: add boss weak point crash interaction
```

---

## 9. 阶段 5：P0 地面冲击波与弱点窗口

### 目标

完成第一条真正的 Boss 战闭环：高弧躲地面攻击，锚点重撞暴露弱点。

### 你负责

1. 创建地面冲击波预警材质；
2. 创建攻击爆发占位材质；
3. 在 Boss 蓝图中配置前摇、攻击和后摇表现；
4. 为 WeakPoint 制作 Protected 与 Exposed 两种明显颜色；
5. 调整锚点位置，使玩家能在攻击后摇中完成反击；
6. 反复试玩并记录：
   - 预警是否看得懂；
   - 高弧是否容易躲避；
   - 弱点窗口是否足够完成重撞。

### 我负责

1. 创建 Boss 最小状态机：
   - Idle；
   - PreparingAttack；
   - Attacking；
   - Recovery；
   - WeakPointExposed；
   - Damaged；
   - Dead；
2. 实现环形地面冲击波；
3. 使用玩家实际高度判断是否受伤；
4. 实现 Player HP、受击无敌时间和死亡；
5. 攻击结束后开放弱点窗口；
6. 弱点窗口结束后恢复保护；
7. Boss HP 为 0 后停止计时器与攻击；
8. 输出攻击、受伤、弱点和死亡日志；
9. 编译验证。

### 验收

- 攻击前有明确预警；
- 留在地面的玩家会受伤；
- 正确时机的高弧跳跃不会受伤；
- 攻击后弱点按时暴露和恢复；
- 玩家能附着锚点并在窗口内完成重撞；
- Boss 三次有效命中后死亡；
- 能从开局完整打完一局。

### Git 检查点

```text
feat: complete ground shockwave boss loop
```

---

## 10. 阶段 6：P0 UI、反馈与核心测试

### 目标

让玩家能读懂当前战斗状态，并为核心闭环建立可重复验证的日志与测试。

### 你负责

1. 创建 `UI/WBP_CombatHUD`；
2. 布置：
   - Player HP；
   - Boss HP；
   - WeakPoint 状态；
   - Crash Charge；
   - Crash Cooldown；
   - Attached 状态；
3. 配置命中闪光、材质变色和简单相机震动；
4. 保证所有反馈优先清楚，不追求复杂美术。

### 我负责

1. 为 UI 提供稳定的 Blueprint Getter 或事件；
2. 建立项目测试日志格式；
3. 实现至少以下测试点：
   - LowArcMovement；
   - HighArcAvoidGroundAttack；
   - AnchorAttach；
   - AttachedVerticalMovement；
   - BossBodyRebound；
   - ProtectedWeakPointNoDamage；
   - ExposedWeakPointDamage；
   - BossDeath；
4. 创建 `tools/parse_log.py`；
5. 生成 `docs/test_report.md`；
6. 编译并运行可执行的自动检查；无法自动化的手感测试标记为 Manual。

### 验收

- UI 与实际状态同步；
- 玩家能明确区分 Low Arc、High Arc 和 Heavy Crash；
- 命中身体、弱点和受伤反馈不同；
- 日志可以被 Python 正确解析；
- 测试报告至少覆盖 8 个核心测试点。

### Git 检查点

```text
test: add combat telemetry and core test report
```

---

## 11. 阶段 7：P1 扇形弹幕

### 目标

让低弧冲刺和地面走位承担明确的防御用途。

### 你负责

1. 创建弹幕球蓝图表现；
2. 配置简单发光材质和拖尾；
3. 调整弹幕数量、间隙和视觉尺寸；
4. 试玩并确认弹幕有压力但存在清楚通路。

### 我负责

1. 创建弹幕 Actor；
2. 实现朝玩家方向的扇形生成；
3. 实现移动、碰撞、伤害和生命周期；
4. 将弹幕加入 Boss 攻击选择；
5. 保证低弧冲刺能可靠穿越或离开弹幕路径；
6. 增加弹幕伤害日志与测试。

### 验收

- 扇形方向容易理解；
- 玩家可通过走位或低弧冲刺躲避；
- 弹幕不会覆盖整个场地；
- 命中只造成一次伤害；
- Boss 死亡后不再生成弹幕。

### Git 检查点

```text
feat: add fan projectile attack
```

---

## 12. 阶段 8：P2 单向激光横扫

### 目标

加入要求玩家判断时机和躲避方式的节奏型攻击。

### 你负责

1. 创建激光路径预警；
2. 创建激光束占位表现；
3. 配置 Boss 蓄力表现；
4. 试玩低位激光的高度和扫速。

### 我负责

1. 实现单向横扫逻辑；
2. 配置预警、启用伤害和结束三个阶段；
3. 使用高度和碰撞共同判定伤害；
4. 将激光接入 Boss 攻击状态机；
5. 增加激光伤害与躲避测试日志。

### 验收

- 预警方向和开始时机清楚；
- 玩家可以跳过低位激光；
- 玩家可以提前低弧冲刺离开路径；
- 激光不会在视觉结束后继续伤害。

### Git 检查点

```text
feat: add sweeping laser attack
```

---

## 13. 阶段 9：P3 锚点干扰

### 目标

限制玩家长期停留在锚点上，使附着成为风险收益选择。

### 你负责

1. 为锚点制作 Normal、Warning、Overloaded 三种视觉状态；
2. 制作简单倒计时反馈；
3. 调整预警时长，保证玩家有机会反应。

### 我负责

1. 统计玩家连续附着时长；
2. 达到阈值后触发锚点过载；
3. 倒计时结束时仍附着则造成伤害；
4. 玩家提前脱离则取消本次伤害；
5. 支持锚点短暂恢复；
6. 增加过载状态日志和测试。

### 验收

- 玩家停留过久时锚点明显变红；
- 提前弹出或脱离可以安全躲避；
- 倒计时结束仍附着会受伤；
- 过载不会永久破坏 P0 战斗闭环。

### Git 检查点

```text
feat: add anchor overload pressure
```

---

## 14. 阶段 10：表现润色与最终交付

### 目标

停止扩展新招式，将现有内容整理成一局可开始、可失败、可胜利、可重开的完整 Demo，并完成反馈、调参、测试和打包。

### 14.1 胜负与重新开始

我负责：

1. 建立 Playing、PlayerDefeated、BossDefeated 和 Restarting 战斗结果；
2. 玩家死亡后停止输入和冲撞；
3. 任一方死亡后停止 Boss 状态机并清理冲击波、弹幕和激光；
4. Boss 死亡后禁止继续受伤；
5. Restart 时重新加载 `Arena_Level`，完整重置 HP、HUD、Anchor 和攻击 Actor；
6. 由 C++ 自动驱动结算 UI，不要求编写 Widget Event Graph。

你负责在 `WBP_CombatHUD` 中增加并保持准确命名：

```text
PANEL_BattleResult
TXT_BattleResult
BTN_Restart
```

`PANEL_BattleResult` 初始 Visibility 设置为 `Collapsed`。

### 14.2 Anchor 过载的世界内反馈

我负责：

1. 为每个 Anchor 创建独立的动态材质实例；
2. 将安全附着和 Warning 进度转换为 `OverloadAlpha`（0–1）材质参数；
3. 随时间让锚点由正常颜色平滑过渡到橙色和红色；
4. Warning 后段提高发光强度和脉冲频率；
5. Recovering 使用碎裂与重生直接表现，不在 HUD 叠加提示；
6. 保证动态生成的多个 Anchor 使用各自独立的材质实例与计时。

你负责制作或调整 Anchor 主材质，使其至少提供：

```text
OverloadAlpha
```

材质使用该参数驱动颜色、Emissive 强度和可选脉冲。Anchor 过载不增加 HUD 文本或进度条。

### 14.3 玩家、身体与弱点命中反馈

我负责：

1. 为玩家受伤、Boss 身体反弹和有效弱点重撞提供不同反馈事件；
2. 支持可配置 Camera Shake；
3. 有效弱点重撞支持 0.03–0.06 秒短 Hit Stop；
4. 保证反馈不会重复结算伤害或破坏冲撞状态。

你负责：

1. 创建 `CS_PlayerHit`；
2. 创建 `CS_BossBodyHit`；
3. 创建 `CS_WeakPointHeavyHit`；
4. 配置玩家闪红、弱点闪光和命中特效；
5. 保证三种命中反馈能明显区分。

### 14.4 攻击与 Anchor 音效接口

我负责为以下时机提供可为空的音效配置槽：

```text
Shockwave Warning / Active
Fan Attack
Laser Warning / Active
Player Hit
Boss Body Hit
Weak Point Hit
Anchor Warning / Shatter
```

你负责整理或制作对应的占位音效；没有资源时允许暂时留空。

### 14.5 参数整理

共同检查：

1. 玩家移动、拖拽瞄准、弧高、速度和冷却；
2. Boss 三种攻击的前摇、范围、速度、伤害和恢复；
3. 弱点窗口和 Boss HP；
4. Anchor 数量、安全时间、Warning、碎裂恢复和伤害；
5. 所有可编辑参数的分类、默认值和 Clamp；
6. 将单局目标时长控制在 60–120 秒。

### 14.6 最终测试、文档与打包

我负责：

1. 修复最终试玩发现的逻辑问题；
2. 清理调试绘制和无用日志；
3. 保留可切换的测试日志；
4. 完善 README、设计说明、测试计划和测试报告；
5. 检查默认地图、GameMode 和打包配置；
6. 执行最终编译、测试和 Git 差异检查；
7. 协助整理面试讲解稿。

你负责：

1. 替换或改善 Boss 占位模型；
2. 完成玩家、Anchor、弱点和攻击材质；
3. 配置 Niagara、音效、灯光和场景表现；
4. 调整 UI 排版；
5. 完成最终手感测试；
6. 录制 60–90 秒演示视频。

### 验收

- Demo 可以稳定完成一局；
- 玩家死亡后显示 Defeat，Boss 停止攻击；
- Boss 死亡后显示 Victory，场上攻击被清理；
- Restart 可以完整重置战斗；
- Anchor 通过材质渐变表达过载进度，碎裂后会在其他随机点补位；
- 玩家受伤、Boss 身体反弹和有效弱点重撞反馈不同；
- 冲击波、弹幕和激光的预警、伤害和视觉时间一致；
- 无阻塞流程的碰撞或状态错误；
- 默认启动进入正确关卡；
- 有 README、设计文档、测试计划和测试报告；
- 有可播放的演示视频；
- 能在 1 分钟内讲清玩法，在 3–5 分钟内讲清工程与测试价值。

### Git 检查点

```text
release: prepare playable boss demo
```

---

## 15. P0 完成定义

只有同时满足以下条件，P0 才算完成：

- [ ] 玩家可以 WASD 移动；
- [ ] 玩家可以低弧冲刺；
- [ ] 玩家可以高弧跳跃；
- [ ] 高弧可以躲避地面攻击；
- [ ] 玩家可以附着指定锚点；
- [ ] 玩家可以在锚点上改变 Z 高度；
- [ ] 玩家可以从锚点拖拽瞄准并重撞；
- [ ] Boss 身体碰撞会反弹但不受伤；
- [ ] Boss 弱点具有 Protected 和 Exposed 状态；
- [ ] 只有有效重撞暴露弱点才扣血；
- [ ] 命中后的反弹由接触点和法线决定；
- [ ] Boss 三次有效命中后死亡并停止攻击；
- [ ] UI 能显示关键状态；
- [ ] 核心规则有日志和测试报告；
- [ ] 可以稳定完成一整局战斗。

P0 未完成前，P1–P4 内容均不得阻塞核心闭环。

---

## 16. 当前下一步

当前进入“阶段 10.2：核心手感与动态 Anchor 验收”。

本轮由 C++ 完成：

1. 左键按下记录拖拽起点，不再按照按住时间蓄力；
2. 拖拽方向决定跳跃方向，拖拽距离同时决定移动距离和弧高；
3. 实时轨迹预览与最终移动使用同一组轨迹参数；
4. 短于 `MinimumDragDistance` 的拖拽取消起跳，避免误触；
5. 随机 Anchor 从出生点下方平滑冒出，冒出完成前关闭附着碰撞；
6. Manager 生成的 Anchor 过载后销毁，不再原地恢复；
7. SpawnManager 延迟选择一个未占用且不同于刚碎裂位置的点补充 Anchor；
8. 手工摆放、没有 SpawnManager Owner 的 Anchor 继续兼容原地恢复。

你负责本轮试玩：

1. 测试左键按住后向目标方向拖动，松开后轨迹是否符合预览；
2. 分别测试短拖、长拖以及从 Anchor 表面起跳；
3. 判断 `MaxDragDistance`、`MinCrashDistance`、`MaxCrashDistance`、
   `MinArcHeight`、`MaxArcHeight` 是否符合手感；
4. 检查 Anchor 冒出期间是否不可附着，完成后是否可正常交互；
5. 让 Anchor 过载，确认碎裂后从其他未占用点冒出替代品；
6. 至少连续碎裂三次，确认场上 Anchor 数量不会减少或重叠。

建议初始参数：

```text
Phase Crash
Max Drag Distance = 800
Minimum Drag Distance = 35
Min Crash Distance = 300
Max Crash Distance = 1000
Min Arc Height = 60
Max Arc Height = 450

Anchor Spawn Manager
Emergence Depth = 400
Emergence Duration = 0.8
Replacement Delay = 2.0
```

---

## 17. 阶段 10.3：Boss 攻击与 Anchor 过载联动

### 目标

让 Boss 攻击、玩家停留和 Anchor 随机补位形成同一个空间决策循环。

### C++ 规则

1. 每个 Anchor 使用持久化的 `CurrentOverloadAlpha`（0–1）；
2. 玩家附着时按秒增加过载，完整过载时间仍由
   `SafeAttachmentDuration + WarningDuration` 决定；
   Warning 阈值按 `SafeAttachmentDuration / 总时长` 计算，不固定为 0.5；
3. 玩家离开后进度保留，不再自动归零；
4. 扇形弹幕命中 Anchor 增加 `0.20`；
5. 激光覆盖 Anchor 时每秒增加 `0.35`；
6. 冲击波暂时不影响 Anchor；
7. 三种来源共享进度并共同驱动材质参数 `OverloadAlpha`；
8. 过载达到 1 时立即碎裂；若玩家正在附着，则受伤并强制脱离；
9. Manager 生成的 Anchor 碎裂后在其他可用随机点补位；
10. 手工摆放的 Anchor 继续在 `RecoveryDuration` 后原地恢复。

### 你负责测试

1. 无人附着时，用五发扇形弹幕使一个 Anchor 过载；
2. 确认单发弹幕约增加 20% 的颜色进度，而且弹幕命中后消失；
3. 让激光扫过 Anchor，确认颜色连续而非一次性跳变；
4. 先让玩家停留积累进度，再由弹幕或激光补满；
5. 玩家中途离开后重新附着，确认进度没有清零；
6. 玩家仍在 Anchor 上时触发碎裂，确认受伤、强制脱离和随机补位；
7. 确认冲击波不会同时破坏场上所有 Anchor。

注意：弹幕与激光只在攻击碰撞体和 Anchor 的 `Attachable Box`
真实相交时增加过载。攻击高度不足不应通过扩大查询范围补偿，应调整攻击生成高度、
激光伤害盒高度或 Anchor 的关卡摆放。

### 初始平衡参数

```text
Fan Projectile Anchor Overload Amount = 0.20
Sweep Laser Anchor Overload Per Second = 0.35
Shockwave Anchor Overload = 0
```

---

## 18. 阶段 11：Boss 分层状态机与攻击决策

详细规则见 `Boss行为设计.md`。

### 当前决定

1. Demo 阶段不引入 UE Behavior Tree；
2. 继续使用 C++ 状态机，但将“行为阶段”和“攻击类型”拆开；
3. Boss 分为 Phase 1（HP 3–2）与 Phase 2（HP 1）；
4. Phase 1 使用带约束权重选择，两次攻击后暴露弱点；
5. Phase 2 使用两段组合攻击，组合结束后暴露弱点；
6. 决策器读取 Grounded、Airborne、Attached 三种玩家空间状态；
7. AimedVolley 负责精确锁定与冲刺测试；高密度扇形留到 Phase 2；
8. 所有攻击必须由视觉和真实碰撞高度共同决定结果。

### 我负责

1. 精简 `EBossEncounterState` 为通用阶段；
2. 新增独立攻击类型与战斗阶段枚举；
3. 实现玩家空间状态识别；
4. 实现防连续重复的权重选择；
5. 实现 Phase 1 攻击计数与弱点窗口；
6. 实现 AimedVolley 的锁定点连续发射；
7. 实现 Phase 2 组合序列；
8. 保持现有蓝图资产和 HUD 接口尽量兼容；
9. 增加选择日志并完成编译验证。

### 你负责

1. 确认 `Boss行为设计.md` 中的阶段与权重是否符合预期；
2. 为 AimedVolley 准备清楚的瞄准预警表现；
3. 试玩确认每种前摇都能在攻击前理解；
4. 调整激光视觉，使 `BeamRoot` 与 `DamageVolume` 高度一致；
5. 记录 Phase 1 和 Phase 2 中不公平或无压力的组合。

### 下一实施步骤

先完成 Phase 1 的状态机重构和权重选择，不同时实现 Phase 2。
Phase 1 验收稳定后，再增加 FanBarrage 和组合攻击，避免一次重构过多系统。

### Phase 1 当前实现状态

- [x] 通用状态与攻击类型拆分；
- [x] Grounded、Airborne、Attached 空间状态识别；
- [x] 三组可编辑攻击权重；
- [x] 禁止连续选择相同攻击；
- [x] 可配置随机种子与完整选择日志；
- [x] 选择时锁定目标位置；
- [x] 两次攻击后暴露弱点；
- [x] HUD 兼容新的状态与攻击类型；
- [x] C++ 与项目蓝图加载检查；
- [x] LowFan 替换为锁定点三发 AimedVolley；
- [x] AimedVolley 弹丸速度提高至 1500；
- [x] Shockwave 扩张时间调整为 1.6 秒；
- [x] 左键按住式输入缓存；
- [x] 右键一次性冲刺输入缓存；
- [x] 跳跃与冲刺 Recovery/CD 缩短；
- [ ] UE5 实机节奏与权重验收；
- [x] Phase 2 只在攻击/弱点窗口边界切换；
- [x] Phase 2 FanBarrage 核心发射调度；
- [x] Ground Pressure：Shockwave → 间隔 → AimedVolley；
- [x] Anchor Pressure：FanBarrage → 间隔 → SweepLaser；
- [x] 两段攻击复用组合开始时的锁定目标；
- [x] 组合结束后必定暴露弱点；
- [x] Phase 2 独立弱点窗口时长；
- [ ] Phase 2 UE5 实机节奏、预警与碰撞验收。

### Phase 2 初始参数

```text
Fan Barrage Warning Duration = 1.0
Fan Barrage Attack Duration = 0.75
Fan Barrage Projectile Count = 13
Fan Barrage Arc Degrees = 90
Fan Barrage Shot Interval = 0.05
Fan Barrage Projectile Speed = 1200
Phase 2 Inter Attack Delay = 0.65
Phase 2 Weak Point Exposed Duration = 2.25
```

这些数值是第一轮可玩测试基线。Phase 2 的压力来自攻击顺序和空间选择，
不通过不可读的极端速度或无间隔叠加制造难度。

Phase 2 第一轮仍根据 Grounded、Airborne、Attached 选择更合适的组合；
后续两种组合强制交替，避免玩家长期保持地面状态时完全看不到
FanBarrage 与 Anchor Pressure。

### Anchor 主动离开规则

玩家从附着状态成功发起跳跃或冲刺后，起点 Anchor 立即进入碎裂流程：

1. 主动离开碎裂不对玩家造成过载伤害；
2. 取消瞄准、无效方向或未真正启动移动时不碎裂；
3. Manager 管理的 Anchor 销毁后按现有规则在其他空闲点补位；
4. 碎裂原因与普通过载分别记录日志，避免重复补位；
5. 被攻击强制脱离仍由原过载规则处理，不重复触发主动离开碎裂。
